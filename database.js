const mysql = require('mysql2');

const config = {
    db: {
        host:'localhost',
        user:'root',
        password:'password123#',
        database:'candy'
    }
}


module.exports = config;

